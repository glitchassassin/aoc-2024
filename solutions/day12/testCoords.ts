import { Direction, rotate } from "../../utils/coords";

let direction = { x: 1, y: 0 } as Direction;

console.log((direction = rotate(direction)));
console.log((direction = rotate(direction)));
console.log((direction = rotate(direction)));
console.log((direction = rotate(direction)));
